// Game constants
const POPULATION_SIZE = 50;
const INPUT_NODES = 4;
const OUTPUT_NODES = 1;
const MUTATION_RATE = 0.1;
const GRAVITY = 0.9;           // Increased gravity
const FLAP_SPEED = -14;        // Stronger flap
const GROUND_HEIGHT = 112;
const PIPE_SPEED = 4;          // Doubled pipe speed
const PIPE_SPAWN_INTERVAL = 1500; // Reduced spawn interval
const PIPE_GAP = 150;
const PIPE_HORIZONTAL_GAP = 220;  // Increased for faster game